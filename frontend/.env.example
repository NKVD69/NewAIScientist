# Backend origin. Omit to use http://localhost:8000.
VITE_API_URL=http://localhost:8000

# Required when the API is started with an API key configured.
# Note this is bundled into the client build, so it is only appropriate
# for local development — do not ship a production key this way.
VITE_API_KEY=
