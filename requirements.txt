# Core LLM + data
openai==1.51.0
# Transitive via openai, pinned explicitly because the FastAPI TestClient and
# any custom transport also need it. Declaring it avoids a partial install
# surfacing as a confusing "No module named 'httpx'" at connection time.
httpx>=0.27.0,<0.28.0
pandas==2.2.3
plotly==5.24.1
arxiv==2.1.3
biopython==1.84

# RAG System dependencies
pypdf==5.1.0
sentence-transformers==3.2.1
chromadb==0.5.15
tiktoken==0.8.0
rank-bm25==0.2.2
pdfplumber==0.11.4

# PDF generation with Unicode support
fpdf2==2.8.1

# API Server (v3.0)
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.17

# DOCX export
python-docx==1.1.2

# Streamlit UI
streamlit==1.39.0

# Note:
# These are loose application-level pins. For fully reproducible builds, generate a
# lock file via `pip-compile requirements.txt` (pip-tools) or `uv pip compile`.
